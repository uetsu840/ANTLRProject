/*
    記述例が正しいか未検証。
    複数行コメントは字句解析が面倒くさいので、なかったことにしても良い。
        ※(*こういうのとか*)　/*こういうのとか*/を含んで解析するには、pushMode/popModeが必須
    配列や構造体はとりあえずなし。
*/
//E1:定数(省略表記)
A := 1; 
A := 1.0; 
A := 'ABC'; //シングルクォートがマルチバイト文字列
A := "ABC"; //ダブルクォートがワイド文字列

//E2:定数(型明示)
A := INT#10;
A := UINT#10;
A := LREAL#1.0;
A := STRING#'ABC';

//E3:定数(2/8/16進表記)
A := 2#1001_0110;
A := 8#1024;
A := 16#ABCD;

//E4:定数(組み合わせ)
A := UINT#2#1100_0011;

//E5:算術演算
A := B + C --D * E / F MOD G ** H;  //単項マイナスに注意

//E6:論理演算
A := NOT B AND C OR D XOR E & F;

//E7:比較演算
A := B < C > D <= E >= F;

//E8:等価比較演算
A := B = C <> D;

//E9:関数
FUNC();
A := FUNC(B);
A := FUNC(B AND C, D = E);
A := FUNC(B := C);
A := FUNC(B := 10, C => 20);